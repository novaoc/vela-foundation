Your order is complete

Order <%= @order.public_reference %> has been fulfilled.
<% @order.line_items.each do |item| %>
<%= item.name %> x <%= item.quantity %> — <%= storefront_money(item.line_total_cents, item.currency) %>
<% end %>
Total: <%= storefront_money(@order.total_cents, @order.currency) %>

View your receipt (private link, expires 24 hours after this receipt was issued):
<%= storefront_order_url(@order.public_reference, access_token: @token, host: Rails.configuration.x.foundation[:domain], protocol: "https") %>

Terms: <%= legal_terms_url(host: Rails.configuration.x.foundation[:domain], protocol: "https") %>
Privacy: <%= legal_privacy_url(host: Rails.configuration.x.foundation[:domain], protocol: "https") %>
